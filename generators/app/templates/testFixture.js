define(['require', 'jquery', 'chai'], function(require, $, chai) {
    var expect = require("chai").expect;

    describe('Unit test template', function() {
        it('should pass', function() {
            expect(true).to.equal(true);
        });
    });
});
